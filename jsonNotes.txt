[To add words to grid]
-addWordToGrid(string word, int direction, int location)
-getDirection(int row, int column)
-addWordToGrid also automatically adds each word to the website wordbank
there is no need to pass in the wb to the JS

for(w : Word)
    addWordToGrid(w.getWord(), getDirection(w.getCoord1, w.getCoord2), (w.getCoord1)*50 + w.getCoord2);

or we can iterate through the array element 1 by 1 if the wordgrid is already created.

int arry1D = 0;
for(i = 0; i < 50; i++)
{
    for(j=0; j < 50; j++)
    {
        A[arry1D] = wordgrid[i][j];
        arry1D++;
    }
}

for(w : Word)
    addWordBank("p5table4, Word);




[To show highlighted choices across clients]
-Broadcast()
-highlightWord()

Somehow recieve the chosen coordinates and communicate
to the other clients and broadcast from the game





[To ]